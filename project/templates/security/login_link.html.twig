{% extends 'base.html.twig' %}

{% block title %}Login link{% endblock %}

{% block body %}
    <h1>Login link</h1>

    {{ form_errors(form) }}

    {{ form_start(form) }}
    {{ form_row(form.email) }}
    <button type="submit" class="btn">Send login link to one user</button>
    {{ form_end(form) }}
{% endblock %}
